// src/config/api.ts
// Centralises the API base so web/native can switch via env without code changes.
// Uses EXPO_PUBLIC_ so the value is embedded at build time for web & native.
// src/constants.ts
export const API_BASE = "https://2eatapp.com/api"; 

